# Private planning + working docs (per global hygiene rules)
CLAUDE.md
plan.md
plan-*.md
PLAN.md
planning.md
plans/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# OS cruft
.DS_Store

# Local secrets / profile (contains personal data — resume PDF, contact info)
profile/
tailored/
secrets.yml

# Outreach work product (personal contact data — kept out of the public repo,
# archived outside git; regenerated ad-hoc so ignore proactively)
data/outreach*
